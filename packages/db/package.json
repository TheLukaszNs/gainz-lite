{
  "name": "@gzlt/db",
  "version": "0.1.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "db:generate": "yarn with-env prisma generate",
    "db:push": "yarn with-env risma db push --skip-generate",
    "db:migrate": "yarn with-env prisma migrate dev --skip-generate",
    "dev": "yarn with-env prisma studio --port 5526",
    "with-env": "dotenv -e ../../.env --"
  },
  "dependencies": {
    "@prisma/client": "^4.8.1"
  },
  "devDependencies": {
    "dotenv-cli": "^6.0.0",
    "prisma": "^4.8.1",
    "typescript": "^4.9.4"
  }
}
